import React from 'react';
import Scene from '../components/game/Scene';

/**
 * Game page - Wrapper for the existing StreamWeave game.
 * This allows the game to be rendered as a route in the SPA.
 */
function Game() {
    return <Scene />;
}

export default Game;
