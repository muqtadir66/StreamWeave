## StreamWeave local env (DO NOT COMMIT REAL SECRETS)
#
# Local development:
# - `npm run dev` (frontend only; no functions)
# - `npm run dev:netlify` (frontend + Netlify Functions)
#
# Netlify production: set these in Site Settings → Environment variables.

# Referee signing key (Solana keypair secretKey array JSON)
GAME_AUTHORITY_KEY=[1,2,3]

# Supabase (server-side only; do NOT expose service role key to the browser)
SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY

